import { CategoriaFiltroPipe } from './categoria-filtro.pipe';

describe('CategoriaFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoriaFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
