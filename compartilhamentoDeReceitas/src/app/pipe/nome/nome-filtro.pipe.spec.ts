import { NomeFiltroPipe } from './nome-filtro.pipe';

describe('NomeFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new NomeFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
